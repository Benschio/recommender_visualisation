/**
 * Created by ru on 21.11.16.
 */
import * as $vec from "./vector2d"
/*import * as $G from "graphinius"

class BarnesHut{

    constructor(private _graph: $G.core.IGraph){

    }

    /*
    * This method takes the graph and (re-)calculates the coordinates for all nodes.
    * The node given as an argument stays in the center of the graph.
    * *//*
    forceLayout(start_node: $G.core.IBaseNode = this._graph.getRandomNode()) :void{

    }
}
export {BarnesHut};*/